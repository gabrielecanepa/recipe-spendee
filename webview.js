module.exports = Franz => {
  document.querySelector("._1EbB").remove(); // Cookies bar
  document.querySelectorAll("g")[4].remove(); // Beta logo
  document.querySelector("._6NCS").remove(); // Membership logo
  document.querySelectorAll(".navItem")[1].remove(); // Redeem list
};
